# Preparation(install git)

```
apt update -y
apt install -y git
```
又は
```
yum update -y
yum install -y git
```

# Install

```
cd
git clone https://github.com/tokane888/dotfiles.git
sudo dotfiles/install.sh
```